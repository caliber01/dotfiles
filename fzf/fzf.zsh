# Setup fzf
# ---------
#if [[ ! "$PATH" == */home/maxim/.fzf/bin* ]]; then
  #export PATH="$PATH:/home/maxim/.fzf/bin"
#fi

# Man path
# --------
#if [[ ! "$MANPATH" == */home/maxim/.fzf/man* && -d "/home/maxim/.fzf/man" ]]; then
  #export MANPATH="$MANPATH:/home/maxim/.fzf/man"
#fi

# Auto-completion
# ---------------
#[[ $- == *i* ]] && source "/home/maxim/.fzf/shell/completion.zsh" 2> /dev/null

# Key bindings
# ------------
#source "/home/maxim/.fzf/shell/key-bindings.zsh"

